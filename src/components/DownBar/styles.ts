import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  z-index: 100;

  width: 100%;
  height: 7rem;
  bottom: 0;

  background: var(--blackTertiary);
`;
